version: "3.9"

services:
  tasks:
    build:
      context: ../services/tasks
    image: techip-tasks:0.1
    container_name: tasks_service
    ports:
      - "8082:8082"
    environment:
      TASKS_PORT: "8082"